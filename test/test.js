const request = require('request');
const assert = require('assert');
const baseurl = "http://localhost:3000/";
const app = require('../app');

describe('shows homepage test', () => {
  describe('GET /', () => {
    it('returns status code 200', () => {

    });
  });
});

